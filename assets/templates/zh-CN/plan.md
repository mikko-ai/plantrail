# 目标

描述本次 run 的目标。

# 非目标

- 

# 受影响模块

- src/

# 步骤

## [step-1] 示例步骤

**描述：** 这个步骤要做什么
**动作类型：** read, write_file
**路径模式：** src/**
**命令模式：** 
**验证：** npm test
**风险：** 低
**回滚：** git checkout -- .
**需要用户确认：** false

# 停止条件

<!--
可选：取消下面两行的注释并填写，批准时会生成 loop_policy，
agent 将自驱动执行直到此命令退出码为 0（或触顶 / 人为终止）。
不填写则本 run 为普通非 loop 流程。

**命令:** npm test
**循环上限:** 10
-->

